import React from 'react'
import classes from './DrawerToggle.css'
const drawerToggle = (props) => (

    <div className = {classes.DrawerToggler} onClick={props.clicked}>menu
    
    <div></div>    
    <div></div>
    <div></div>

    </div>

    )

export default drawerToggle